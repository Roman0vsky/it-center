@using ITCenterBack.Models;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model ITCenterBack.ViewModels.BasicViewModels.AdministrationViewModel
@{
	Layout = "/Views/Shared/_AdminLayout.cshtml";
	ViewBag.Title = "Изменить администрацию";
}

<div class="admin-page__content">
	<div class="admin-page__content-form">
		<div class="admin-form">
			<div class="admin-form__item">
				<h2 class="heading">Редактировать </h2>
			</div>
			@using (Html.BeginForm("Update", "Administration", FormMethod.Post, new { enctype = "multipart/form-data" }))
			{
				@Html.HiddenFor(model => model.Id)
				@Html.HiddenFor(model => model.IsHeadOfThecenter)
				@Html.HiddenFor(model => model.IsAdministrator)

				<div class="admin-form__item">
					<label class="input__label">
						Введите имя <br />
						@Html.EditorFor(model => model.Name) <hr />
					</label>
					<label class="input__label">
						Введите ссылку <br />
						@Html.EditorFor(model => model.Link) <hr />
					</label>
					<label class="input__label">
						Введите описание <br />
						@Html.EditorFor(model => model.Description) <hr />
					</label>
					<label class="input__label">
						Изображение <hr />
						<img src=@Model.Image alt="images">
						@Html.HiddenFor(model => model.Image)
						<br />
						Обновить изображение
						<div class="editor-field">
							<input type="file" name="uploadedFile" accept="image/png, image/jpeg, image/gif, image/bmp" />
						</div>
					</label>
				</div>

				<div class="admin-form__item buttons">
					<input class="primary-button" type="submit" value="Сохранить" />
				</div>
			}

			<div class="admin-form__item buttons">
				<a class="secondary-button" href=@Url.Content("/api/Administration/All")>Отмена</a>
			</div>
			<div class="admin-form__item"></div>
			<div class="admin-form__item"></div>
		</div>
	</div>
</div>